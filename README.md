# Instructions

## Start development server:

- `npm install`
- `npm start`

## Production build

- `npm install`
- `npm run build`

The result will then be in the `build` folder

## Tests

- `npm install`
- `npm test`
